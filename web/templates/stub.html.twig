{# SPDX-License-Identifier: AGPL-3.0-only #}
{% extends 'base.html.twig' %}
{% block body %}
<section class="pad"><div class="wrap" style="max-width:760px">
  <div class="kicker">{{ page_title }}</div>
  <h1 class="disp">{{ page_title }}</h1>
  <p class="lead" style="margin-top:1rem">This page is being built in a later migration phase.</p>
</div></section>
{% endblock %}
