# Defaults for running the Symfony API. In the dev container these are
# overridden by docker compose (see developers/docker/compose.yaml).
# This file is committed and must NOT contain real secrets.
APP_ENV=dev
APP_DEBUG=1
APP_SECRET=dev-not-secret-change-me

###> symfony/framework-bundle ###
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
# Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
# See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
# Format described at https://www.doctrine-project.org/projects/doctrine-dbal/en/latest/reference/configuration.html#connecting-using-a-url
# IMPORTANT: You MUST configure your server version, either here or in config/packages/doctrine.yaml
#
# Points at the `db` service on the compose network by default.
DATABASE_URL="postgresql://cc:cc@db:5432/cyclingcommons?serverVersion=16&charset=utf8"
###< doctrine/doctrine-bundle ###

###> symfony/mailer (used in a later plan) ###
# MAILER_DSN=null://null
###< symfony/mailer ###

###> app runtime config ###
# Public site origin, used for canonical/OG URLs in prod.
APP_SITE_URL=https://cyclingcommons.org
# Mapillary access token — set the real value ONLY in .env.local
MAPILLARY_TOKEN=
###< app runtime config ###
